[build]
  command = "npm run build"
  publish = "dist"

# Single-page app: every route must be served index.html so that deep links,
# refreshes and shared links resolve instead of 404ing.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
